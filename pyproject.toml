[project]
name = "openreelpy"
version = "1.0.0"
description = "Open-media video discovery and streaming with FastAPI"
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "app:app"

[tool.pytest.ini_options]
testpaths = ["tests"]
